@model PerformanceDetailsViewModel

<div class="articles-read">
    <div class="image img-thumbnail">
        @*<img src="@Model.ImageUrl" class="img-fluid" width="800" height="600" alt="@Model.Title">*@
        @*<img src="@Model.ImageUrl" class="rounded mx-auto d-block" max.width: 100% height="500" alt="@Model.Title">*@
        <img src="@Model.ImageUrl" class="img-fluid" alt="@Model.Title">
        <div class="article">
            <h3 class="text-center" style="color:#ba9765">@Model.Title</h3>
            <div class="content text text-center font-weight-normal">
                <p>@Model.Synopsis</p>
            </div>
        </div>
        @*@if (Model. != null)
        {
            <iframe class="mb-3" width="560" height="315" src="@Model.VideoUrl"
                    title="YouTube video player" frameborder="0"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                    allowfullscreen>
            </iframe>
        }*@
    </div>
</div>

@*display: block;
  margin-left: auto;
  margin-right: auto;
  width: 50%;*@